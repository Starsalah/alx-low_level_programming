#include "main.h"

/**
 * _abs - gets an integer's absolute value
 * @n: the integer to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
